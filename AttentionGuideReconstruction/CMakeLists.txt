aux_source_directory(. ATTENTIONGUIDERECONSTRUCTION_LIST)
add_library(ATTENTIONGUIDERECONSTRUCTION_LIST ${ATTENTIONGUIDERECONSTRUCTION_LIST})

find_package(PCL REQUIRED)

target_link_libraries(ATTENTIONGUIDERECONSTRUCTION_LIST ${PCL_LIBRARIES})
